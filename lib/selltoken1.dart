import 'package:flutter/material.dart';

class selltoken1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Select Page'),
      ),
      body: Center(
        child: Text('Welcome to the Select Page!'),
      ),
    );
  }
}
